PORT=3000
API_PREFIX=api

# Firebase – Service Account: caminho para .json OU conteúdo do JSON em base64
# Exemplo path: ./firebase-service-account.json
# Exemplo base64: colar o resultado de: cat serviceAccountKey.json | base64 -w0 (Linux) ou certutil -encode (Windows)
GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json
FIREBASE_PROJECT_ID=cooka-28201
FIREBASE_STORAGE_BUCKET=cooka-28201.firebasestorage.app
# API Key (para refresh token Firebase) – use a chave Web ou a do android/ios
FIREBASE_API_KEY=AIzaSyDg1syHBjNf9SQOlJ08nZkdCGgxF60ddeI

# JWT (tokens emitidos pelo backend para refresh em mobile)
JWT_SECRET=change-me-in-production-min-32-chars
JWT_ACCESS_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_DAYS=7
